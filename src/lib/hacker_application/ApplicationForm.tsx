'use client';

import { ApplicationData } from './types';

/**
 *
 * appData can be locally cached or a new empty one.
 */
export function ApplicationForm({ appData }: { appData: ApplicationData }) {}
